package com.meta.wearable.dat.externalsampleapps.sightread.storage

object StorageLimits {
  const val MAX_CONVERSATIONS = 50
  const val MAX_MESSAGES_PER_CONVERSATION = 200
  const val MAX_IMAGE_BYTES = 100 * 1024 * 1024L
}
